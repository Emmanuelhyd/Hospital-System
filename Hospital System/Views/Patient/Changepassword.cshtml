@model Hospital_System.Models.Patients
    @{
        ViewBag.Title = "Changepassword";
        Layout = "~/Views/Shared/_PatientLayout.cshtml";
    }



    @*@ViewBag.Message

    @using (Html.BeginForm())
    {
        <div>
            <h2>Change Password</h2>

            <div>
                @Html.LabelFor(m => m.UserName)
                @Html.TextBoxFor(m => m.UserName, new { @placeholder = "UserName", @class = "form-control" })
                @Html.ValidationMessageFor(m => m.UserName, "", new { @class = "text-danger" })
            </div>

            <div>
                @Html.LabelFor(m => m.Password, "NewPassword")
                @Html.PasswordFor(m => m.Password, new { @placeholder = "New Password", @class = "form-control" })
                @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
            </div>*@



@*<div>

        @Html.LabelFor(m => m.Password, "ConfirmPassword")
        @Html.PasswordFor(m => m.NewPassword, new { @placeholder = "Confirm Password", @class = "form-control" })
        @Html.ValidationMessageFor(m => m.NewPassword, "", new { @class = "text-danger" })
    </div>*@

@*<div>
                <input type="submit" value="Confirm" class="btn btn-submit" />
            </div>
        </div>


    }*@


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>@ViewBag.Title</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 630px;
            height:400px;
            margin: 30px auto;
            padding: 20px;
            background: #fff;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }

        h2 {
            text-align: center;
            color: #333;
        }

        .form-group {
            margin-bottom: 20px;
        }

            .form-group label {
                display: block;
                font-weight: bold;
                margin-bottom: 5px;
                color: #333;
                margin-left: 70px;
                position: absolute;
                margin-top: 50px;
            }

            .form-group input[type="text"],
            .form-group input[type="password"] {
                width: 40%;
                padding: 10px;
                border: 1px solid #313ed3;
                border-radius: 4px;
                box-sizing: border-box;
                margin-left: 200px;
                margin-top: 43px;
            }

            .form-group .text-danger {
                color: red;
                font-size: 0.875rem;
                margin-top: 5px;
                margin-left: 213px;
                font-weight: 600;
                font-style: oblique;
            }

        .btn-submit {
            display: block;
            width: 55%;
            padding: 10px;
            font-size: 1rem;
            color: #fff;
            background-color: #007bff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s;
            margin-top: 45px;
            margin-left: 124px;
        }

            .btn-submit:hover {
                background-color: #0056b3;
            }
    </style>
</head>
<body>
    <div class="container">
      

        @using (Html.BeginForm())
        {
            <div>
                <h2>@ViewBag.Title</h2>
                <div style=" color: red; margin-left: 247px; font-weight: 600; font-style: oblique;">
                    @TempData["valid"]
                </div>

                    <div class="form-group">
                        @Html.LabelFor(m => m.UserName)
                        @Html.TextBoxFor(m => m.UserName, new { @placeholder = "UserName", @class = "form-control" })<br />
                        @Html.ValidationMessageFor(m => m.UserName, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(m => m.Password, "NewPassword")
                        @Html.PasswordFor(m => m.Password, new { @placeholder = "New Password", @class = "form-control" })<br />
                        @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
                    </div>

                    <input type="submit" value="Change Password" class="btn-submit" />
                </div>
                }
            </div>
</body>
</html>




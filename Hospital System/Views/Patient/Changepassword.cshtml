@*@model Hospital_System.Models.Custom
    @{
        ViewBag.Title = "Changepassword";
    }

    @ViewBag.message;
    @using (Html.BeginForm())
    {
    <h2>Forgotpassword</h2>
    <div style="margin-left: 402px; position: absolute; width: 195px; height: 33px; ">

        @Html.Label("Oldpassword", "Oldpassword:")
        @Html.TextBoxFor(m => m.Password, new { @placeholder = "Password", id = "name" })
        @Html.ValidationMessageFor(m => m.Password, validationMessage: null, htmlAttributes: new { @class = "text-danger" })

        <br />
    </div>

    <div style="margin-left: 402px; position: absolute; width: 195px; height: 33px; margin-top: 50px; ">
        @Html.Label("Newpassword", "Newpassword:")
        @Html.PasswordFor(m => m.NewPassword, new { @placeholder = "Password", id = "password" })
        @Html.ValidationMessageFor(m => m.NewPassword, validationMessage: null, htmlAttributes: new { @class = "text-danger" })
        <br />
    </div>

    <div style=" margin-left: 402px; position: absolute; width: 195px; height: 33px; margin-top: 100px; ">
        @Html.Label("ConfirmPassword", "ConfirmPassword:")
        @Html.PasswordFor(m => m.ConfirmPassword, new { @placeholder = "Password", id = "password" })
        @Html.ValidationMessageFor(m => m.ConfirmPassword, validationMessage: null, htmlAttributes: new { @class = "text-danger" })
        <br />
    </div>

    <div style="margin-left: 402px; position: absolute; width: 195px; height: 33px; margin-top: 200px;">
        <input type="submit" value="Confirm" />
        <br />

    </div>


    }*@


@model Hospital_System.Models.Custom
@{
    ViewBag.Title = "Change Password";
    Layout = "~/Views/Shared/_PatientLayout.cshtml";
}

@ViewBag.message;

@using (Html.BeginForm("ChangePassword", "Patient", FormMethod.Post, new { @class = "change-password-form" }))
{
    <div>
        <h2>Change Password</h2>

        <div class="form-group">
            @Html.Label("OldPassword", "Old Password:")
            @Html.PasswordFor(m => m.Password, new { @placeholder = "Old Password", @class = "form-control" })
            @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
        </div>

        <div class="form-group">
            @Html.Label("NewPassword", "New Password:")
            @Html.PasswordFor(m => m.NewPassword, new { @placeholder = "New Password", @class = "form-control" })
            @Html.ValidationMessageFor(m => m.NewPassword, "", new { @class = "text-danger" })
        </div>

        <div class="form-group">
            @Html.Label("ConfirmPassword", "Confirm Password:")
            @Html.PasswordFor(m => m.ConfirmPassword, new { @placeholder = "Confirm Password", @class = "form-control" })
            @Html.ValidationMessageFor(m => m.ConfirmPassword, "", new { @class = "text-danger" })
        </div>

        <div class="form-group">
            <input type="submit" value="Confirm" class="btn btn-submit" />
        </div>
    </div>
}

<style>
    .change-password-form {
        max-width: 600px; /* Restrict form width for readability */
        margin: 0 auto; /* Center the form horizontally */
        padding: 20px;
        background-color: #f9f9f9; /* Light gray background */
        border-radius: 8px; /* Rounded corners */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow for depth */
        border: 1px solid #ddd; /* Light border */
    }

    .form-group {
        margin-bottom: 20px; /* Space between form groups */
    }

        .form-group label {
            display: block;
            font-size: 1rem; /* Larger font size for labels */
            color: #333; /* Darker text color */
            margin-bottom: 8px; /* Space between label and input */
        }

    .form-control {
        width: 100%;
        padding: 10px;
        font-size: 1rem;
        border: 1px solid #ccc; /* Light border */
        border-radius: 4px; /* Rounded corners */
        box-shadow: inset 0 1px 2px rgba(0, 0, 0, 0.1); /* Inner shadow */
        transition: border-color 0.2s ease; /* Smooth border color transition */
    }

        .form-control:focus {
            border-color: #007bff; /* Blue border on focus */
            outline: none; /* Remove default outline */
            box-shadow: 0 0 0 0.2rem rgba(38, 143, 255, 0.25); /* Blue shadow on focus */
        }

    .text-danger {
        color: #dc3545; /* Red color for error messages */
        font-size: 0.875rem; /* Slightly smaller font size */
        margin-top: 5px; /* Space between input and error message */
    }

    .btn-submit {
        display: inline-block;
        padding: 10px 20px;
        font-size: 1rem;
        color: #ffffff; /* White text color */
        background-color: #007bff; /* Blue background color */
        border: none;
        border-radius: 4px; /* Rounded corners */
        cursor: pointer; /* Pointer cursor on hover */
        transition: background-color 0.3s ease; /* Smooth background color transition */
    }

        .btn-submit:hover {
            background-color: #0056b3; /* Darker blue on hover */
        }
</style>
